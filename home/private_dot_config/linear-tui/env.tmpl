{{- /* Rendered only where OpenBao is reachable (see .chezmoiignore). The key
       never touches the source repo — it is pulled from Bao at apply time. */ -}}
{{- $key := output "sh" "-c" "BAO_ADDR=https://bao.olm-hops.ts.net bao kv get -field=api_key kv/projects/linear 2>/dev/null || true" | trim -}}
LINEAR_API_KEY={{ $key }}
